#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

int main () {
    // int n = 4;
    // std :: cout << std::pow(n, 2) << " " << std::sqrt(n);
    // cout << pow(n, 2) << " " << sqrt(n);
    
    // vector<int> arr;
    // vector <int> arr(3);
    // vector <int> arr[3];
    // int arr[3];
    
    arr.resize(10);
    for (int i = 0; i < 10; i++)
        arr[i] = i + 1;
    return 0;
}